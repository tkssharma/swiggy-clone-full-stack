import { extendTheme } from "@chakra-ui/react";

const CustomFont = extendTheme({
  fonts: {
    heading: `sans-serif,ProximaNova,arial,Helvetica Neue`,
    body: `sans-serif,ProximaNova,arial,Helvetica Neue`,
  },
});

export default CustomFont;
